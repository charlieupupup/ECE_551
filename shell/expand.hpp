#ifndef _EXPAND_VECTOR_HPP_
#define _EXPAND_VECTOR_HPP_

#include <string>
#include <vector>
#include <map>

std::vector<std::string> expand(std::vector<std::string> input, std::map<std::string, std::string> var);

#endif