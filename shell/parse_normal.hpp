#ifndef _PARSE_NORMAL_HPP_
#define _PARSE_NORMAL_HPP_

#include <string>
#include <vector>
#include <map>

std::vector<std::string> parse_normal(std::string input, std::map<std::string, std::string> var);

#endif
